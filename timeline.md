# NeuroStack-LM experiment timeline

## 2026-07-26

- Began by reading the project brief and Experiment 1 specification.
- Found an empty workspace with no existing Git repository or implementation.
- Decision: treat the broad project brief as scientific motivation and the Experiment 1 document as the implementation contract. Build the smallest auditable end-to-end pilot that preserves the requested comparisons and measurements; do not invent substitute datasets or silently claim the full confirmatory study.
- Downloaded the official bAbI v1.2 release and pinned the fixed Qwen2.5-0.5B-Instruct revision.
- Built and ran a one-seed diagnostic pilot with a parameter-matched generic baseline.
- Result: Stage 1 acceptance failed because supporting-fact selection was weak, routing collapsed, and retrieval/verifier acceptance metrics were unavailable. Per the preregistration, later wake/sleep results are diagnostic only.
- Added explicit research provenance for every current mechanism or scaffold. Key decision: do not equate a named head with an implemented biological mechanism; fast plasticity and differentiated modulation remain unimplemented.
- User requested periodic publication to `SirNosh/neurostack-lm`; repository was confirmed empty and GitHub authentication is active.
- Published the complete reproducibility package to GitHub in commit `9ea3e45`, including Git LFS checkpoints, cached frozen features, metrics, and example-level predictions.
- Research decision: use PBWM, shared global workspace, sparse MoE, Neural Episodic Control/DNC, Backpropamine, PRM process supervision, and complementary learning systems as algorithmic bases. Clearly mark reduced proxies and unimplemented mechanisms instead of relying on anatomical names.
- Tagged the audited negative pilot as `pilot-v0-negative` and pushed the tag.
- Implemented the Stage 1R stateful mechanism core and token-level frozen-Qwen adapter hooks.
- Added 25 deterministic tests (including six integration/lifetime tests); all pass.
- Ran a real-Qwen two-cycle smoke lifetime: four-slot broadcast, two scoped event writes, retrieval in both sessions, and nonzero rank-8 fast state all executed with the backbone frozen.
- Added shared-resource coordination under the name `neurostack-lm`; no long training job will start without an active reservation and calibrated duration.
- Decision: do not start the 18-run qualification matrix until dataset adapters, R0/R1/R2 baselines, and a short resource calibration exist. Execution success is not a qualification result.
- Audited the mechanism core against the prequalification review. Corrected the R3 fairness issue with a same-sized unrestricted 5-to-12 controller, moved routing to the current contextual state after layer 5, and made working-memory and workspace decisions differentiable.
- Made retrieval breadth row-specific and evaluation-time episodic writes model-controlled. Added explicit bootstrap losses for working memory, workspace admission, and episodic write targets.
- Decision: count the current adapter bank honestly as dense four-expert execution. Sparse routing does not imply sparse compute until dispatch itself is sparse.
- Implemented the exact common `Stage1RExample` schema. Froze deterministic bAbI qa1-qa5 manifests (25,000 train, 2,500 dev, 5,000 official test) and the official BABILong 4K qa1-qa5 training selection (10,000 examples) at dataset revision `b3513ef7c25c54ce706054530d47668c532019d6`.
- Decision: do not invent BABILong supporting-fact annotations because the official training rows expose only input, question, and target.
- CPU validation now passes 42 tests, including an integrated two-cycle task-loss gradient test. The real-Qwen hash-verified smoke is waiting for the shared GPU ledger to become free.
- The integrated gradient test exposed an in-place PFC strategy-reset update that invalidated autograd version tracking. Replaced it with an out-of-place slot scale; the task loss now reaches working-memory operation/address heads and the workspace scorer.
- Re-ran the bounded real-Qwen lifetime after obtaining an exclusive ledger reservation. It passed in about 43 seconds at 0.962 GB peak CUDA allocation; the in-memory backbone SHA-256 was identical before and after, scoped recall returned one event per session, and the FLOP audit counted all four densely evaluated experts.
- Implemented R0 as four parameter-matched generic adapters on the same frozen Qwen token path and insertion layers. The real-Qwen three-pass smoke matched R5's 11,373,945 trainable parameters with 11,374,788 parameters (0.0074% error), preserved the backbone hash, used 0.964 GB peak CUDA allocation, and counted 681,676,800 adapter matmul FLOPs.
- Froze official PRM800K data at commit `7ecc794703b2877f63226f2477a49b34f9b25163`: 50,000 positive and 50,000 negative training steps plus 5,000/5,000 development steps, with complete source problems assigned to exactly one split.
- Froze official FewRel data at commit `278a2315d2138810a379cd8d5718914dc56e2582`: 64 meta-train and 16 held-out relations, with 1,000 deterministic 5-way episodes for each of 1-shot and 5-shot per partition.
- Fairness decision: R3 and R5 must receive identical controller inputs and downstream losses. A secondary R3+aux condition will match R5's auxiliary-prediction capacity without imposing semantic channel assignments.
- Began the audited next milestone under an append-only shared-resource reservation for `neurostack-lm` (CPU RAM and disk only; no GPU yet).
- Implemented FewRel v2 without replacing the frozen v1 artifact: deterministic 48/16/16 relation partitions, independently permuted episode-local A-E labels, visible support labels, hidden query labels, and instance-disjoint support/query examples.
- Decision: make the FewRel local plasticity contract explicit in a small qualification head. Support representations are keys and learned A-E episode-label embeddings are values; evaluation updates detached fast state only and supports the shuffled-DA control.
- Replaced primary R0's repeated identical inputs with four projected generic feedback tokens derived from the previous pass mean state. The zero-feedback lesion retains the same token count and compute shape.
- Expanded the preregistered contract to seven systems by adding R3+aux, producing exactly 21 system/seed entries. R1 and R2 remain explicitly unavailable until implemented; capacity reports must not fabricate their measurements.
- Added a standardized parameter/capacity audit and an R5 sleep-capacity-matched narrower R0 ordinary-replay control. CPU validation passes 59 tests before artifact generation and real-Qwen measurement.
- The real-Qwen iterative R0 audit passed after waiting for and reserving the next free GPU window. Successive pass logits differed by max-absolute 17.25 and 13.625; the learned-feedback versus zero-feedback final-logit delta was 14.25. Parameter mismatch remained 0.0074%, the backbone hash was unchanged, and peak CUDA allocation was 0.960 GB.

## 2026-07-28

- User authorized the remaining CPU, RAM, disk, and GPU runs without a resource cap. The append-only shared-resource ledger remains authoritative for coordination: implementation work was claimed under `neurostack-lm`, and GPU use will receive its own just-in-time claim and release.
- Acquired and pinned the official EPBench, CLUTRR, TRACE, Multi-Session Chat, and BABILong sources needed by the preregistered protocol. Decision: build bounded deterministic manifests from these sources and preserve their native split/session/task boundaries; do not expand the experiment with new tasks or mechanisms.
- The implementation order remains deliberately narrow: freeze missing adapters, implement R1/R2, add one recoverable runner, calibrate R0/R2/R5, then run the R5-first qualification wave. A failed foundational gate will be reported as a scientific result rather than concealed by extra tuning.
- Published commit `c485005` with the five remaining official manifests, conventional R1/R2 controls, measured capacity accounting, source-paper/code provenance, and 66 passing tests.
- The first GPU launcher left a duplicate R0 child after its timeout; terminated only that exact duplicate. Its sibling later encountered an illegal CUDA access, so the contaminated attempt was discarded. A clean rerun passed the same point without error.
- Caught and corrected a calibration-panel counting error before accepting results: TRACE is 128 rows for each of four tasks, not all 500 rows per task. Repeated R0 from the beginning after verifying the corrected 1,536-example family counts.
- Exact R0/R2/R5 calibration passed for seed 1729. All three completed 20 warm-up, 100 measured developmental, 256 wake, 25 sleep, full 1,536-item evaluation, checkpoint save/restore, and repeated-batch equivalence. Backbone hashes remained unchanged and peak CUDA allocation was 2.51/2.64/2.68 GB respectively.
- Calibration interpretation: these first-token mixed-panel accuracies are execution diagnostics, not qualification scores. The calibration runner must not be used to claim support selection or the other absolute mechanism gates without their explicit objectives and metrics.
- Before launching R5 seeds, audited the runner against the actual gate surfaces. Found that token-level supporting-fact scores were not exposed and the FewRel local fast learner was not owned by R5. Added only those missing qualification paths rather than treating mixed-panel next-token accuracy as evidence.
- Corrected EPBench scoping: events and questions from one book now share the same `epbench` task context, allowing the required session-scoped retrieval. Regenerated the manifest and capacity audit.
- Added the frozen 4,000-step Stage 1 mechanism-bootstrap schedule to the common runner: microbatch 2, accumulation 8, exact family probabilities, controller/adapter learning rates, cosine warmup, checkpoint recovery, support/retrieval/working-memory/verifier/router/modulator losses, and full bAbI/PRM800K/EPBench development gates.
- Qualification smoke validation passed for an accumulated optimizer step (2.66 seconds on the RTX 4070 SUPER) and for the complete gate evaluator on a tiny source-backed panel. Decision: the expected roughly three-hour Stage 1 run per seed justifies staged R5 execution and periodic checkpoints; no baselines launch unless all R5 foundational gates pass.
- Repeated the complete R0/R2/R5 calibration after adding the gate surfaces. All three again passed exact checkpoint resume, finite-loss, frozen-backbone, and VRAM-headroom checks. Updated peak allocations are 2.511, 2.638, and 2.687 GB; the exact readiness commit can now be frozen without relying on stale artifacts.
- Froze and pushed `stage1r-prequalification-ready` at commit `2a4b402`, then rechecked and extended the shared ledger before starting R5 seed 1729. The first 1,000 of 4,000 Stage 1 optimizer steps completed with finite objectives, stable ~6.3 GB physical GPU use, and valid recovery checkpoints at steps 500 and 1,000. Recent mixed-batch answer loss fell from ~15 near step 100 to ~1.8 near step 1,017; gate interpretation remains withheld until full development evaluation.
- R5 seed 1729 passed step 1,500 with its third recovery checkpoint. Training stayed near 2.2 seconds per optimizer step, ~6.3 GB physical GPU use, and finite support/retrieval/verifier/router/working-memory objectives; recent mixed answer loss remained around 1.9.
- R5 seed 1729 crossed the 2,000-step halfway checkpoint with no OOM, non-finite loss, or resource drift. Recent answer loss was ~1.9, support loss had remained near 0.08–0.11, and the cosine-decay phase continued at the preregistered schedule.
- R5 seed 1729 wrote its step-2,500 checkpoint (62.5%). Physical GPU use remained ~6.3 GB at ~45°C, step time ~2.2 seconds, and recent mixed answer/support losses were ~1.8/~0.09.
- R5 seed 1729 wrote its step-3,000 checkpoint (75%). No recovery, numerical, or thermal intervention was needed; recent answer loss was ~1.7 and the support objective remained near 0.09.
- R5 seed 1729 wrote its step-3,500 checkpoint (87.5%). It remained near 2.2 seconds/step with recent answer loss ~1.9 and no GPU/resource drift.
- R5 seed 1729 completed all 4,000 preregistered Stage 1 optimizer steps and wrote the final training checkpoint at 04:25:34. Full bAbI/PRM800K/EPBench development evaluation began immediately; no thresholds were inspected or altered during training.
- R5 seed 1729 completed full development evaluation and failed five of six foundational checks: support AUPRC 0.342, episodic recall@4 0.0118 with -0.4-point answer gain, verifier AUROC/F1 0.627/0.593, collapsed router mass, and only one workspace source used. Working-memory non-collapse passed. Decision: preserve the negative seed, run the other two preregistered R5 seeds, and do not launch attribution or conventional-baseline waves if the foundational failure replicates.
- Published the complete seed-1729 artifacts in commit `ef245ea`. After rechecking the ledger, started R5 seed 2718; its first 500 steps and recovery checkpoint completed with the same stable ~2.2-second step time and finite losses.
- R5 seed 2718 crossed step 1,000 and wrote its second recovery checkpoint at 05:27:56. Recent answer loss was ~1.9; no resource or numerical divergence from seed 1729 was observed.
- Seed 2718 stopped at step 1,172 when one microbatch sampled the exact same bAbI example twice: retrieval events were scoped by shared example session while target masks were row-local. Changed only the event filter to the already-recorded row provenance, reproduced the duplicate case successfully with backward gradients, and reran all 67 tests. Seed 1729 never hit the duplicate case; its computation is unaffected. Resume from the step-1,000 checkpoint is required.
- Published the duplicate-row recovery as commit `a87c72c` and tag `stage1r-prequalification-ready-v2`. Seed 2718 resumed from step 1,000 with restored RNG/optimizer/scheduler state, passed the former crash point, and wrote its step-1,500 checkpoint with recent answer loss ~1.65.
- R5 seed 2718 reached the durable step-2,000 halfway checkpoint after recovery. Recent answer loss was ~1.88 at ~2.17 seconds/step, with no repeat of the duplicate-row failure or other numerical/resource anomaly.
- R5 seed 2718 wrote its step-2,500 checkpoint (62.5%) and continued beyond it. Recent answer loss was ~1.55 with stable throughput; the resumed optimizer/RNG trajectory remained finite and recoverable.
- R5 seed 2718 wrote its step-3,000 checkpoint (75%) and continued beyond it. Recent answer loss was ~1.88; no further recovery, numerical, or resource intervention was needed.
- R5 seed 2718 wrote its step-3,500 checkpoint (87.5%) and continued beyond it. Recent answer loss was ~1.60 with stable throughput and no resource drift.
- R5 seed 2718 completed all 4,000 preregistered Stage 1 optimizer steps after exact checkpoint recovery. The final recent answer loss was ~1.79; full bAbI/PRM800K/EPBench development evaluation began without changing any gate or threshold.
- R5 seed 2718 completed full development evaluation. Support selection (AUPRC 0.354), episodic retrieval (recall@4 0.0257; 0.0-point answer gain), and verifier (AUROC/F1 0.600/0.563) failed. Router balance, working-memory non-collapse, and four-source workspace utilization passed. Decision: this independently replicates seed 1729's three central failures; preserve the result and run the third preregistered seed before stopping the wave.
- Published the complete seed-2718 artifacts in commit `9ecfa53`, rechecked the append-only resource ledger and live GPU state, and started the third preregistered R5 seed 3141. It wrote recovery checkpoints at steps 500, 1,000, and 1,500; at step 1,617 recent answer loss was ~1.82 with stable ~2.28-second steps.
- The one-hour foreground command window later closed seed 3141 at its step-1,800 progress print; this was process supervision, not a model/runtime failure. Rechecked the ledger and idle GPU, recorded an `ACTIVE RECOVERY` entry, and resumed in a detached process from the durable step-1,500 checkpoint with restored weights, optimizer, scheduler, and RNG state.
- Seed 3141's detached recovery passed the former stop point and wrote its durable step-2,000 halfway checkpoint. Recent answer loss was ~1.79 with stable ~2.3-second steps and no model, numerical, or resource failure.
- Seed 3141 wrote its step-2,500 checkpoint (62.5%) and continued beyond it. Recent answer loss was ~1.76 with stable throughput; exact recovery remained healthy.
- Seed 3141 wrote its step-3,000 checkpoint (75%) and continued beyond it. Recent answer loss was ~1.75 with no numerical, model, or resource anomaly.
- Seed 3141 wrote its step-3,500 checkpoint (87.5%) and continued beyond it. Recent answer loss was ~1.83 with stable throughput and no resource drift.
- Seed 3141 completed all 4,000 preregistered Stage 1 optimizer steps after exact checkpoint recovery. Final recent answer loss was ~1.66; the full frozen bAbI/PRM800K/EPBench gate evaluation began without any threshold or setting change.
- Seed 3141 completed full evaluation and replicated the foundational negative: support AUPRC 0.355, episodic recall@4 0.0107 with -0.2-point answer gain, and verifier AUROC/F1 0.617/0.574 all failed. Working-memory non-collapse and three-source workspace utilization passed; router balance failed.
- Aggregated all three preregistered R5 seeds. Support AUPRC was 0.350 mean (0.342-0.355), episodic recall@4 0.0160 (0.0107-0.0257), answer gain -0.2 points (-0.4-0.0), verifier AUROC 0.615 (0.600-0.627), and verifier macro-F1 0.577 (0.563-0.593). Decision: apply the frozen stop rule and do not launch R3/R4 attribution or R0/R1/R2 baseline waves.
- Reran the complete validation suite (67 passed), confirmed no Python process and an idle display-only GPU, and appended the final `neurostack-lm` RELEASED event to the shared resource ledger.
- User requested publication of all remaining local results. Decision: publish the four untracked result archives on the existing `codex/experiment2` branch; they total about 7.3 MB, so additional compression is unnecessary. No unrelated worktree changes were present.
